def evaluate(weather_data):
    flags = []
    if not weather_data:
        return flags
    
    # Placeholder for weather specific rules. 
    # Current integration relies on sensor_rules and disease_rules compounding with weather data.
    return flags
